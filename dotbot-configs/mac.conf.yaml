- shell:
    - [ brew bundle --file Brewfile, Install dependencies]
    -
        command: sudo easy_install pip
        stdin: true
        description: Installing pip
    - [ /usr/local/opt/fzf/install --key-bindings --completion --no-update-rc, Install fzf (Fuzzy Find) ]
    -
        command: sudo chsh -s /bin/zsh
        stdin: true
        description: Making zsh the default shell
